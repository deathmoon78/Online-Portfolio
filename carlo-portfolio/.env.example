# === AI Provider (use ONE of the two below) ===

# Option A — Groq (free tier, fast Llama-3.1-70B)
GROQ_API_KEY=xxx

# Option B — OpenAI (GPT-4o-mini, fallback if no Groq key)
# OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx
