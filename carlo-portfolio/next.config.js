/** @type {import('next').NextConfig} */
const nextConfig = {
  // Serve static files from /public — avatar frames live at /public/avatar-frames/
  // No extra config needed; Next.js serves /public automatically.
  // This file is here for any future customisation.
};

module.exports = nextConfig;
